import React from "react";
import { Container } from "react-bootstrap";

const Footer = () => {
  return (
    <div className="py-2 border-top bg-blur">
      <Container fluid>
        <div>FOOTER</div>
      </Container>
    </div>
  );
};

export default Footer;
